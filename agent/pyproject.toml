[project]
name = "sysint-agent"
version = "1.0.0"
description = "SysInt AI Agent — powered by Azure OpenAI"
requires-python = ">=3.12"

dependencies = [
    "fastapi>=0.136.1",
    "openai>=2.35.1",
    "python-dotenv>=1.2.2",
    "uvicorn>=0.46.0",
]

